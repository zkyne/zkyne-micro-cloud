server:
  port: 10041
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri:
          search-paths:
#          username: 公有仓库不用配置
#          password:
      label: master